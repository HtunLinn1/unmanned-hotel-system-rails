<nav class="navbar navbar-expand-lg navbar-dark header fixed-top" id="navbar">
  <div class="container-fluid">
    <div class="d-flex justify-content-start">
      <div class="header-logo">
        <a aria-current="page" href="/" style="color: white;">
          <%= image_tag "hotel-logo.jpg", class: "hotel-logo" %>
          7Stars Hotel
        </a>
      </div>
    </div>
    <!-- トグルボタン -->
    <button class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <!-- リンク一覧 -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <div class="header-logo pt-2">
            <% if user_signed_in? %>
              <%= link_to("#{current_user.name}", "/users/#{current_user.id}", class: 'user', method: :get) %>
            <% end %>
          </div>
        </li>
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="/facilities/index" style="color: white;">
            設備案内
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/smartlocks/index" style="color: white;">スマートロック</a>
        </li>
        <% if user_signed_in? && !current_user.staff %>
          <li class="nav-item">
            <a class="nav-link" href="/inquiries/index" style="color: white;">お問い合わせ</a>
          </li>
        <% end %>
        <li class="nav-item">
          <a class="nav-link" href="/faqs" style="color: white;">よくある質問</a>
        </li>
        <% if user_signed_in? && current_user.staff %>
          <li class="nav-item">
            <a class="nav-link" href="/rooms" style="color: white;">部屋</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/bookings" style="color: white;">予約リスト</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/bookings/history" style="color: white;">予約履歴</a>
          </li>
        <% end %>
          <% if user_signed_in? %>
            <% if !current_user.staff %>
              <li class="nav-item">
                <a class="nav-link" href="/bookings" style="color: white;">予約リスト</a>
              </li>
            <% end %>
            <li class="nav-item pt-2">
              <%= link_to("ログアウト", "/users/sign_out", method: :delete, data: {confirm: "本当にログアウトしますか？"}) %>
            </li>
          <% else %>
            <li class="nav-item pt-2">
              <%= link_to("アカウント作成", "/users/sign_up") %>
              <%= link_to("ログイン", "/users/sign_in") %>
            </li>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>
